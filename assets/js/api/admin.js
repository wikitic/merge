/*
import axios from 'axios';

export default {
    patchAdmins (admin) {
        return axios.patch(
            '/api/v1/admins/'+admin.id,
            {
                username: admin.username,
                password: admin.password
            }
        )
    }
}
*/